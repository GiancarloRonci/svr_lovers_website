// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Se pubblichi su https://<utente>.github.io/<nome-repo>/, imposta:
  // site: 'https://<utente>.github.io',
  // base: '/<nome-repo>',
  // Se il repo si chiama <utente>.github.io (sito principale), lascia base non impostato.
});
